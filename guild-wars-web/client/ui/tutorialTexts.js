// Tutorial text library. Keys match the trigger site call: show(key, TUTORIAL_TEXTS[key]).
// Add entries here when wiring tutorial triggers in App.js or UIManager.js.
export const TUTORIAL_TEXTS = {
  // Example:
  // 'terrain.firstRegionSelect': 'Click a region and choose its terrain type from the panel.',
}
